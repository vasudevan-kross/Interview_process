# New Feature

Guide me through adding a new feature to this project following all repo conventions.

Read the skill at `.claude/skills/interview-platform-feature/SKILL.md` and follow it to help with any combination of:

- **Database migration** — new SQL file + update the consolidated schema
- **Backend API endpoint** — FastAPI route with auth, multi-tenancy, rate limiting
- **Frontend dashboard page** — Next.js page with Supabase auth, skeleton loading, design system

The user's request: $ARGUMENTS
