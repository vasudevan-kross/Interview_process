# New Migration

Create a new Supabase migration for this project.

Read the "Database Migration" section of `.claude/skills/interview-platform-feature/SKILL.md` and follow it exactly.

Key rules:
1. Check `backend/migrations/` for the highest-numbered file and use the next number
2. Create `backend/migrations/0NN_description.sql`
3. Apply the same changes to `backend/migrations/000_consolidated_schema.sql` in the correct section

The migration to create: $ARGUMENTS
